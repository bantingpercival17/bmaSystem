<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="{{ public_path('css/app-pdf.css') }}">
    <title>@yield('title-report')</title>
    @yield('style')
</head>

<body>
    <header>
        <span class="form-code">BMA FORM @yield('form-code')<span>
                <center>
                    <img src="{{ public_path() . '/assets/image/report-header.png' }}" alt="page-header">
                </center>

    </header>
    <div class="page-content">
        @yield('content')
    </div>

    <footer>

        <div class="timestamp">
            {{-- <table class="table-content" >
                <tr>
                    <td><small>GENERATED BY: <b>{{ strtoupper(Auth::user()->name) }}</b></small></td>
                    <td>
                        <small>
                            GENERATED DATE: <b>{{ now() }}</b>
                        </small>
                    </td>
                </tr>
            </table> --}}
            <small>
                @if (Auth::user())
                    GENERATED BY: <b>{{ strtoupper(Auth::user()->name) }}</b>
                @endif

            </small> <br>
            <small>GENERATED DATE: <b>{{ now() }}</b></small>
        </div>
    </footer>

</body>

</html>
